// particles.min.js
(function(window, document) {
    function particlesJS(selector, options) {
        console.log('Particles initialized for', selector, 'with options', options);
        // Custom particle logic here
    }

    particlesJS.load = function(selector, path, callback) {
        // Mock implementation for loading configuration from a path
        console.log('Loading particles configuration from', path);
        // Example: simulate loading and initializing particles
        setTimeout(function() {
            particlesJS(selector, {}); // Pass loaded configuration here
            if (callback) callback();
        }, 1000);
    };

    window.particlesJS = particlesJS;
})(window, document);
